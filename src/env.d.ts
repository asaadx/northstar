/// <reference types="vite/client" />

/**
 * Injected by Vite's `define`, fixed for the lifetime of a dev-server process.
 * Only read behind `import.meta.env.DEV`.
 */
declare const __DEV_RUN__: string;
